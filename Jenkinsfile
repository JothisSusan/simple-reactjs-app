pipeline {
    agent {
        docker {
            image 'node:alpine:3.7'
            args '-p 3000:3000'
        }
    }
    stages {
        stage('Build') {
            steps {
                sh 'npm install'
            }
        }
    }
}
